All information as of 2025-11-19, 13:00.

In this first version of the software, control of the hourly loading is still done via cronjobs.

## wikiproj
User `wikiproj@clsrv`
```
# m h  dom mon dow   command
30 * * * * /home/wikiproj/prod/qa/run_db_qa.sh
15 * * * * /usr/bin/flock -n /tmp/run_orch_fixfiles.lockfile ./prod/orch/run_orch_fixfiles.sh
```
User wikiproj has 
* orch version commit id `17b579e`
* wiki-eventstreamer commit id `e206212`
* qa version commit id `c878077`

## wikidata
User `wikidata@clsrv`
```
# m h  dom mon dow   command
10 * * * * /home/wikidata/orch/run_orch.sh
```
in directory `./orch/`, git commit id is `997c944`

The cronjob downloads the data files via rsync and stores the name of the file in a table. Based on the information in that table the program `./prod/orch/run_orch_fixfiles.sh` executed by `wikiproj` picks up and loads all data files that are not marked as loaded in the table into the SQL db.
