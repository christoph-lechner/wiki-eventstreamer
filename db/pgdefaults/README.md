Default configuration file `postgresql.conf` generated by the configuration in `docker-compose.yaml`.
Thanks to volume mapping, this file is stored on the host system (in the path holding the actual database, so superuser privileges are needed for reading the config file).
