This directory contains the configuration for postgreSQL v18.

To boost performance when running complex queries, the memory footprint was tweaked to a total RAM usage of about 4GB (physical RAM of the machine is 16GB, but it is not used for this project exclusively).
